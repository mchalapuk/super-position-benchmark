group = 'pl.chalapuk.superposition'
version = '0.1'

description = 'Benchmark of SuperPosition utility (state sharing without resource locking in Java).'

task wrapper(type: Wrapper) {
    gradleVersion = '3.3'
}

